/* pages/me/favorites/index.wxss · 我的收藏
   视觉对标 my-favorites.html，全部走 app.wxss 新 token，禁硬编码配色 */
.fav-scroll {
  box-sizing: border-box;
  height: 100vh;
  background: var(--paper);
}
.fav-scroll .container {
  padding: 0 28rpx 40rpx;
}

/* ============ Hero ============ */
.fav-head {
  padding: 96rpx 0 0;
}
.fav-title {
  margin-top: 8rpx;
  font-family: var(--sans);
  font-size: var(--fs-hero-lg);
  font-weight: 900;
  line-height: 0.95;
  letter-spacing: -4rpx;
  color: var(--ink);
}
.fav-title .pop {
  color: var(--pop);
}
.fav-count {
  margin-top: 22rpx;
  font-size: var(--fs-sub);
  font-weight: 600;
  color: var(--mut-strong);
}

/* ============ Tabs ============ */
.fav-tabs {
  display: flex;
  gap: 54rpx;
  margin-top: 42rpx;
  padding-bottom: 0;
  border-bottom: 2rpx solid var(--paper-2);
}
.fav-tab {
  position: relative;
  padding-bottom: 24rpx;
  font-size: 32rpx;
  font-weight: 800;
  letter-spacing: -0.4rpx;
  color: var(--mut-strong);
}
.fav-tab.on {
  color: var(--ink);
}
.fav-tab.on::after {
  content: "";
  position: absolute;
  left: 0;
  right: 0;
  bottom: -2rpx;
  height: 6rpx;
  border-radius: 6rpx;
  background: var(--pop);
}
.fav-tab-badge {
  margin-left: 8rpx;
  font-size: var(--fs-mini);
  font-weight: 700;
  color: var(--mut);
}

/* ============ body ============ */
.fav-body {
  padding-top: 38rpx;
}

/* 菜谱 2 列网格 */
.fav-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 29rpx;
}
.fav-grid-cell {
  min-width: 0;
}
/* recipe-card virtualHost 根节点在格子内铺满 */
.fav-grid-cell .rc-card {
  width: 100%;
}

/* ============ 帖子行卡（对标 .pcard）============ */
.fav-pcard {
  display: flex;
  gap: 27rpx;
  margin-bottom: 27rpx;
  padding: 25rpx;
  border-radius: var(--r-card);
  background: var(--c-surface);
  border: var(--bd-card);
  box-shadow: var(--sh-card);
}
.fav-pthumb {
  width: 177rpx;
  height: 177rpx;
  border-radius: var(--r-md);
  flex: none;
  background: var(--paper-2);
}
.fav-pmid {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}
.fav-ptag {
  align-self: flex-start;
  font-family: var(--serif);
  font-style: italic;
  font-size: var(--fs-mini);
  letter-spacing: 1rpx;
  font-weight: 700;
  color: var(--gold-deep);
}
.fav-ptitle {
  margin-top: 6rpx;
  font-size: 32rpx;
  font-weight: 800;
  line-height: 1.25;
  letter-spacing: -0.6rpx;
  color: var(--ink);
}
.fav-pby {
  display: flex;
  align-items: center;
  gap: 14rpx;
  margin-top: auto;
  padding-top: 14rpx;
}
.fav-pav {
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex: none;
  font-size: var(--fs-mini);
  font-weight: 800;
  color: var(--c-surface);
}
.fav-pnm {
  flex: 1;
  min-width: 0;
  font-size: var(--fs-cap);
  font-weight: 600;
  color: var(--mut-strong);
}
.fav-plk {
  flex: none;
  display: flex;
  align-items: center;
  gap: 7rpx;
  font-size: var(--fs-cap);
  font-weight: 700;
  color: var(--pop);
}
.fav-plk-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  background: var(--pop);
  flex: none;
}
/* 加载骨架 */
.fav-skel-cover {
  width: 100%;
  height: 260rpx;
  border-radius: var(--r-card);
  background: var(--paper-2);
}
.fav-skel-line {
  margin-top: 14rpx;
  height: 24rpx;
  border-radius: 12rpx;
  background: var(--paper-2);
  width: 80%;
}
.fav-skel-line.strong { width: 60%; }
.fav-pane-skel { display: flex; flex-direction: column; gap: 20rpx; }
.fav-pcard-skel {
  height: 180rpx;
  border-radius: var(--r-card);
  background: var(--paper-2);
}
