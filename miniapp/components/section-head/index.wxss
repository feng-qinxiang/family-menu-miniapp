.sh-comp { align-items: flex-end; }
.seeall { display: flex; align-items: center; gap: 4rpx; }
.seeall-hint { line-height: 1; }
.seeall-chev {
  flex: none;
  margin-left: 2rpx;
  color: var(--gold-deep);
  font-size: 34rpx;
  font-weight: 500;
  line-height: 1;
  transform: translateY(-1rpx);
}
