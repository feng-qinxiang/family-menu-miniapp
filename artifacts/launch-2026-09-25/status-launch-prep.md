# 上线准备复核 · 2026-09-25（R11）

本轮目标：以上线为前提，把「能在本机落地」的上线六项全部做完并留证据；其余项标注真实边界。

## 1. 门禁读数（对着当前工作区重跑）

| 门禁 | 命令 | 读数 |
| --- | --- | --- |
| 前端 11 条 | `node miniapp/test/*.test.js` + static-check / interaction-audit / ui-ledger | 全部 exit 0 |
| static-check preflight | 同上 | 点名 2 处占位符：`env.js` trial/release、`legal-config.js` 运营者信息（属预期提醒） |
| 后端全量 | `DB_PASSWORD=… ./mvnw -B test`（先 DROP 测试库） | **223 项 / 0 失败 / 0 错误，BUILD SUCCESS**（种子修复后复跑同读数） |

## 2. 管理后台（SMS_PROVIDER=noop，未接真实网关）

- 登录页：验证码登录 + 引导令牌双通道，页面如实提示「本环境可能未接短信网关，没收到验证码就用『引导令牌』」。
- 验证码路径（浏览器真点）：填 13800138000 → 「获取验证码」→ 按钮进入 60s 冷却、页面 alert 显示
  「调试验证码（仅开发环境）：246810」→ 填码登录 → 进入数据看板（真实统计/趋势/热门内容/最近注册）。
- bootstrap 路径（API）：`POST /api/admin/auth/bootstrap` 正确令牌 → 200 + 会话 token（userId=1 超级管理员）；
  错误令牌 → 401。
- 页面走查：用户管理（5 账号表格、搜索、导出、分页、行内操作）、审计日志（空 = 无写操作，符合「写操作才留痕」）。
- 越权：无令牌 `GET /api/admin/dashboard` → 401。
- 结论：**不接真实网关时后台可运营**（dev OTP / bootstrap 两条路），真实网关只需注入
  `SMS_PROVIDER=tencent` + 5 项密钥，代码已就位（`TencentSmsGateway`）。

## 3. 生产库迁移彩排（脚本零改动）+ 一键脚本

- 临时库 `schema.sql` → 27 张表。
- 3 个迁移（`migrate-import-source-index` / `migrate-post-feed-index` / `migrate-cook-history-family-index`）
  各连跑 2 遍：exit 0，自检 EXPLAIN 命中索引、无 filesort。
- 把 3 个索引 DROP 后重跑（存量库「缺索引」路径）：全部正确重建，幂等成立。
- **新增 `server/sql/migrate-prod.sh`**：把手册四步串成一条命令（备份 → 执行 → 校验 → 计划抽查，
  `set -e` 失败即停，备份 dump 留在当前目录兜底）。已在旧索引形状的临时库整体彩排通过
  （备份 32K → 三条迁移 rc=0 → 4 条索引列序正确 → 最重查询 `type=ref` 无 filesort）。
- **生产库当前不存在**（`docs/上线实施方案.md`：服务器/域名/备案是 M0 阻断项，SSH 无任何主机）。
  服务器买好后两条路任选：
  ```bash
  # 一键（推荐，含备份与校验）
  bash server/sql/migrate-prod.sh <用户名> <数据库名>
  # 或按手册分步（spec/LAUNCH-CHECKLIST.md §三条索引迁移的执行手册）
  ```

## 4. 三主线实操巡场（开发者工具自动化，iPhone 12/13 Pro 390×844，后端 = 本地 9088）

- **点菜**：首页 hero「加到菜单」真点 → 卡片「还没点菜」→「今日菜单 · 晚餐 糖醋排骨」（缩略图正常），
  「买」按钮红点角标出现（清单自动派生）。
- **做菜**：菜谱详情（食材清单 家里有 0/5、缺 N 样加入买菜清单）→「开始做菜」进沉浸模式（暗底、
  步骤配料跟随、进度段可点跳步）。计时：第 3 步起 10 分钟 → 09:58 在走；跳第 4 步 →
  后台条「第三步还剩 09:29 · 回去›」按墙钟续走（R4 行为在位）。最后一步「完成 · 记一笔」→
  cook_history 落库 → redirect 做菜记录页（本月 1 道 / 本周打卡 1/7）。
- **做完菜扣冰箱（UI 全流程）**：给测试家庭造库存 鸡蛋 10 个 / 番茄 5 个 → UI 做番茄炒蛋（食材 鸡蛋 3 个/番茄 2 个）→
  库存变 鸡蛋 7 / 番茄 3；冰箱页正确显示 + 番茄「还剩 3 天，先用」临期提醒。
- **社区**：feed（统计卡/帖子卡带图）→ 帖子详情（关联菜谱卡双向在位）→ 点赞 toggle（计数按行重算，
  符合 `syncCommunityLikeCount` 设计）→ 发评论（落库 PENDING = 机审兜底先审后发，作者可见）。
- **console**：全程 0 error / 0 warn。
- **测试残留**：全部清除（cook_history ×3、family 库存 ×2、今日菜单+清单、点赞、评论），开发库计数按行重算。

## 5. 本轮修复

- `data-demo.sql`：种子 like/comment 计数改为「真实背书行 + 从行派生」，幂等判重；
  临时库 3 连跑计数稳定。详见 CHANGELOG「Fixed」首条。开发库脏计数（128/96/75）已重算回真实值。

## 6. 真机三项：前置已全部打通，只差扫码操作

**本轮新打通的链路**（此前真机预览会因 `develop=localhost` 连不上任何后端而全程断网）：

- `utils/env.js` 新增真机开发版分支：`develop` 环境 + 非 devtools 平台 → 走 `DEV_LAN_BASE`
  （当前预填 `http://192.168.1.83:9088`，Mac 局域网 IP 变了要更新）。开发者工具行为不变
  （运行时实测：devtools→localhost、mock 真机→局域网地址）；static-check 门禁不受影响。
- 本地服务已在 `*:9088` 监听（`bash server/dev-server.sh start|status|stop`——重启电脑后
  手机连不上就先跑 `start`；启动成功会顺带打印手机要连的局域网地址）。
- `auto_preview` 已把开发版（主包 1.31MB / 总 1.74MB）推到登录开发者工具的微信账号。

**手机操作步骤**（真机三项的公共前置）：
1. 手机和电脑连同一个 Wi-Fi。
2. 微信里打开刚收到的开发版预览（或扫开发者工具「预览」二维码）。
3. 点右上角「…」→ **打开调试**（开发版不校验合法域名，局域网 http 才能通；首页 hero 能出真实菜谱即连通）。
4. 若连不上：确认 `DEV_LAN_BASE` 里的 IP 还是电脑当前 IP（`ipconfig getifaddr en0`，
   查不到再试 `en1`——2026-09-25 实测同一台 Mac 的地址会从 en0 漂到 en1，IP 本身没变），
   且 macOS 防火墙没有拦 Java（系统设置 → 网络 → 防火墙，放行或临时关闭）。
5. 预览过期（超过 ~24h）就重推：`wechatide -c ZCode auto_preview --project <项目路径>/miniapp`
   （本机 CLI 已授权；后端没起就先 `bash server/dev-server.sh start`）。

**三项核对清单**（做完在前后打钩，别用模拟器结论冒充）：
- [ ] **相册授权弹窗**：菜谱编辑 → 选封面图 → 首次应弹「相册/摄像头」权限弹窗；拒绝后再点，
       应回导「去设置开权限」而不是没反应；同意后选图回显正常。
- [ ] **深色模式整体回归**：手机系统切深色 → 逐页看首页/菜谱/详情/做菜沉浸页/社区/冰箱/我的：
       卡片表面、文字、图标是否可读（重点：做菜沉浸页是恒定暗底，浅色 token 全被替换过）；
       红底按钮上的图标在深色档仍是浅色。
- [ ] **小屏弹层内滚动**：小屏机型（或开发者工具切 SE 尺寸后真机验证手感）→ 社区发帖弹层、
       家庭邀请弹层：内容能滚到底、取消/发布按钮完整可见、不被 tabBar 吃掉。
       （工具端已验过 390×844 弹层 lift 修复；SE 尺寸与真机手感是这三项的本意。）

## 7. 仍需 owner 的（代码无法代劳）

1. `legal-config.js` 运营者姓名 + 联系方式（**只有本人能填，代填即与微信实名不符**；本轮已确认暂保持占位）。
2. ICP 备案域名 → `env.js` trial/release（约定：后续再搞；备案下来前真机验证用 §6 的局域网链路）。
3. 真实短信网关：腾讯云控制台开短信 → 注入 `SMS_PROVIDER=tencent` + 5 项环境变量。
4. 买服务器后：`bash server/sql/migrate-prod.sh <用户名> <数据库名>`（§3 已彩排）。
5. 真机三项：按 §6 清单扫码核对（前置链路已打通，开发版已推到手机）。
