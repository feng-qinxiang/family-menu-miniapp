# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
小程序尚未提审，所以还没有版本号；**第一个 tag 会在提审那天打**，此前的改动都记在「未发布」里。
每条都能对应到一次提交和 `spec/LAUNCH-CHECKLIST.md` 里的一行证据。

## [未发布]

### Added

- **注销账号入口（提审合规硬项）**：隐私政策承诺「账号注销后 15 个工作日内删除或匿名化」，
  此前全站没有任何注销入口，这句承诺没有可执行通道。新增页面 `pkg-extra/legal/deletion`
  （排版与字号 token 复用隐私政策，样式走 `@import "../privacy/index.wxss"`，不复制第二份）：
  四段说明（注销后会怎样 / 如何申请 / 15 个工作日的处理时限 / 注销前请确认），
  底部是运营者联系方式（取自 `utils/legal-config.js` 单一来源，点一下复制）。入口在
  「我的 → 设置 → 关于 → 注销账号」，隐私政策「数据保留」一段也补了这条路径。
  **不做自助注销**：前端提交 + 后端端点属于需要拍板的产品项，与后端回执一致
  （后端现提供人工匿名化流程，见 `spec/LAUNCH-CHECKLIST.md` §6.4）。
- **发帖层补上内容规范提示**：正文输入框下面一行小字——「请勿发布违法违规、侵权、广告或与美食无关的
  内容；违规内容会被删除，账号可能被封禁」。UGC 页面提审要求「发布前可见的规范告知」，与后端的
  msgSecCheck/imgSecCheck 机审、运营后台的举报审核队列配套。样式复用全局 `.muted` + `--fs-cap`
  字号 token，大字模式跟着放大（`pages/community/index.wxml` 的 `.pf-hint`）。
- **「晒到邻里厨房」：把刚出锅的菜直接交给社区发帖层**。厨房总控的「已上桌」卡与菜谱详情的
  「大家晒的」都有入口，点一下切到社区 tab 并打开发帖层：标题预填「<菜名> 出锅了」、
  发帖层里多一行「这道菜 <菜名>」的关联（可点 × 取消）。tab 页 `wx.switchTab` **带不了 query**，
  所以走一次性 storage 交接（`utils/post-share.js`：取一次就撕、跳转失败也撕）。
- **帖子关联菜谱之后双向可见**：帖子里长出菜谱卡（点进菜谱），那道菜的详情页也新增「大家晒的」
  区块（最多 3 条 + 「晒我的作品」入口，动线成环）。新读路径 `GET /api/community/posts?recipeId=N`
  审核语义与信息流一致（APPROVED 公开 / PENDING 仅作者本人可见），关联菜谱的可见性规则与另外三条
  读路径**共用同一处映射**（`CommunityPostsByRecipeTests` 4 条，含"别人家的私房菜不随帖外泄"）。
  执行计划实测（2000 行合成数据、其中该菜 37 帖）：两段各是 `Index lookup on p using idx_post_recipe`，
  外层 derived 只有 5 行，整条 0.3ms。
- **`miniapp/test/post-share.test.js`**（零依赖，已挂 `miniapp-ci` 第 3 步）：钉住跨 tab 交接的两条契约——
  取一次就撕（社区页 `onShow` 会反复触发：从帖子详情返回、切 tab 回来都算）、跳转失败也要撕
  （留着它，用户下次自己点开社区会凭空弹出发帖层，还带着一道早不记得的菜）。
  反向验证过：把 fail 分支的 `clearPending()` 去掉，第 ④ 组断言当场失败。
- **做完菜自动扣冰箱库存**：`POST /api/cook-history` 会按菜谱标准用量回写 `pantry_item`
  （名称与单位都要对得上、"适量/少许"不扣、同料多行临期优先、扣光即删行）。
  响应新增可选字段 `pantryDeducted`，「上桌」提示会写"冰箱扣了 N 项"。
- **`/healthz` 健康检查**：只回 `UP`/`DOWN`（数据库不通时 503），不外泄内部信息；已纳入限流。
- **四条前端门禁扩到 16 项静态检查**，本轮新增：暗底页前景在两档都可读（12）、
  动态标题不许用单行紧凑行高（13）、按真实字节报主包/分包体积（14）、
  大字模式每页都必须接线（15）、`catch` 清列表却不置失败位即失败（16）。
- **社区/后台的私密披露与模板**：`SECURITY.md`、Issue 模板（问题反馈 / 功能建议）。
- **静态检查扩到 19 项**（第六轮 R1）：18) WXML 用到的自定义组件必须在同页 `index.json` 声明
  （漏声明不报错、整屏不渲染）；19) JS 里置的 `*Failed` / `*Error` 状态位必须真的被本页 WXML 读到
  （置了没人读 = 失败被显示成空态）。两条都反向验证过能判失败。
- **静态检查扩到 20 项**（第六轮 R2）：20) WXML 里的图标不许用 emoji——第 17 项只管 WXSS 的 `content:`，
  管不到渲染层，所以 kitchen 的 💡🎉 活了一整轮。B 分支按 Unicode `Emoji_Presentation=Yes` 的码位清单判定，
  不用整段 26xx/27xx（否则 `✓ ★ ✦ ❝ ⚑` 这些默认文字呈现、本来就能着色的字符全被误判）。
- **`app.wxss` 补一份全站共享的 CSS 图标词汇**：`.ico-bubble`（评论）/ `.ico-share`（分享）/ `.ico-flag`（举报），
  一律 `currentColor` + `em` 单位（跟主题与激活态翻色、跟大字模式缩放）。
- **UI 台账 `miniapp/test/ui-ledger.js`**（零依赖、生成物、已挂 `miniapp-ci` 第 5 步）：逐页输出图块边长、
  圆角取值、加载态、空态、裸 rpx/px、emoji 数、未声明组件、置了没人读的状态位，产出
  `artifacts/ui-ledger/ledger.md|json`。主指标是「还没进 token 表的裸值种数」，要单调下降。
- **图块与头像收进一张规格表**：`--tile-row-sm/--tile-chip/--tile-cover/--tile-grid/--tile-pick`
  与 `--ava-row/--ava-head/--ava-cmt`，11 处裸值归位（同一角色的菜图曾出现 96/104/112/120/146 五种）。
- **静态检查扩到 21 项**：21) 类名带 `thumb` 的图块边长必须走规格表，不许写裸 rpx（反向验证过能判失败）。
- **社区信息流 SQL 重写 + 分页不变量测试**（`CommunityFeedUnionTests`，2 条，反向验证过有牙）：
  待审帖只对作者可见这个 OR 原本让 `idx_post_audit` 退化成 filesort，现拆成 UNION ALL 两段各自命中索引。
- **`ShoppingListSourceTests`**（2 条）：钉住清单条目的"来自哪道菜"确实回指用到它的菜谱，
  以及每条都带 `sourceRecipes` 数组（前端按 `.length` 判断，给 null 会让那一行整个消失）。
- **`AdminUiAssetsTests`**（7 条，纯静态、毫秒级、随 `mvn verify` 跑）：后台前端是零构建的裸资源，
  没有打包器/lint/类型检查，这轮把"机器一眼能看出来、但只有打开浏览器才会暴露"的错误钉住——
  空态行 colspan 与表头列数一致（列数写错只在线上第一次返回空时才露出来）、
  x 轴标签走均匀抽样（不许再手写补末尾标签，实测会把 09-21 与 09-22 贴成 `09-2109-22`）、
  卡片横向兜底还在、页内不重复渲染标题、时间列不折行、`td.clamp` 都带 `title`、
  引导登录不退回折叠区且可用性由服务端渲染（不许前端打接口探测）、调试探针没有残留。

### Fixed

- **演示种子的点赞/评论数不再是无背书的假数字**：`data-demo.sql` 此前直接写
  `like_count=12/8`、`comment_count=3/1`，但没有对应的点赞行/评论行——而运行时的
  点赞 toggle 是「从行重算」（`MysqlKitchenStore#syncCommunityLikeCount`，防并发漂移），
  第一次真实点赞就会把 12 当场塌成 1，feed 按 `like_count` 排的顺序和卡片数字对不上，
  社区首页「N 条评论」的合计也是假的。改为：计数从 0 起步、种子账号的点赞/评论落
  **真实行**、再从行数派生计数（与运行时同一口径）。评论表没有
  `(post_id, user_id)` 唯一键，幂等靠 `NOT EXISTS` 按内容判重——首版用 `INSERT IGNORE`
  在临时库三连跑里把评论翻倍（2→4），即改。已在临时库验证：连跑 3 遍计数稳定
  3/2、3/1；开发库的一次性脏计数（128/96/75）也已按行重算回真实值。
  （本文件只影响 `APP_SEED_DEMO_DATA=true` 的本地/演示库，生产由 profile 固定关闭。）
- **删除库存拿到 404 时按「已删除」处理**：后端把 `DELETE /api/pantry/{id}` 从「一律 200」
  改成「影响 0 行返回 404」（重复删同一条 / 两端同时删 / 删别人家的）。两处调用点
  （`pages/pantry`、`pages/shopping` 的库存区块）共用 `utils/api.js` 的 `deletePantryItem`，
  在这个共享函数里吞掉 404 并按成功返回——否则连点两次删除，第二次明明已经删掉了却闪
  「删除失败」。请求同时标记 `silent`，免得 api 层先把服务端的 404 文案弹出来；真失败
  （500 / 断网）照旧抛出，由调用方原有的 `catch` 提示，失败提示反而从两条收敛成一条。
  防回归：`test/pantry-delete.test.js`（4 组断言，已挂 `miniapp-ci`）。
- **`COMMUNITY` 开关补彻底（个人主体 / 企业主体的上线分岔）**：个人主体开不了「社交-社区/论坛」
  类目，社区 UGC 要能整体下线。开关此前只覆盖 tabBar、通知、我的页收藏，本轮补齐：
  社区首页（`onLoad` + `onShow` 双守卫）、帖子详情（分享卡片能直达）、收藏页、
  首页的社区菜谱并流、菜谱详情的「大家晒的 + 晒我的作品」、厨房总控的「晒到邻里厨房」，
  以及所有「晒一晒」动线唯一的出口 `utils/post-share.js`（关闭时直接 reject，
  漏改的入口最坏只是一次失败调用，不会把用户送进发帖层）。
  实测（模拟器）：开关置 false → tabBar 只剩 4 项、直达 `/pages/community/index` 被弹回首页、
  菜谱详情不再出现「大家晒的」、菜名来源标签从「社区」变「精选」；置回 true 全部恢复。
  防回归：`static-check` 新增「社区开关覆盖面」（12 个入口的守卫被删掉会当场失败，
  截图 `artifacts/launch-2026-09-23/miniapp/90~92-community-*.png`），
  `post-share.test.js` 新增第 ⑤ 组断言（社区关闭时不写一次性交接单、不跳社区 tab）。
- **后台表格画到卡片外面、右侧「操作」列被切掉**：宽表的兜底从 `@media (max-width:1240px)`
  改成 `@container`，阈值挂在 `.panel-root` 上（卡片宽 = 它的内容宽，大屏下 `.main` 比卡片宽，
  挂 `.main` 会误判成放得下）。旧阈值漏在**量的是视口而不是内容区**：视口 1280 时内容区只有
  992px，媒体查询不触发，9 列的今日菜单要 1079px，实测溢出 149px、整页横向滚动 142px。
  `--cell-clamp` 300px→220px 让最宽的表在 1440 窗口下也能塞进卡片。
  实测（无头 Chrome，1280/1440/1920 三档逐页跑 14 页）：三档下 `pageScroll=0`；1440/1920 下卡片
  不产生横向滚动、表头照常吸顶（滚到 900px 时表头停在顶栏底边 y=72）；1280 下内容区只有 981px
  触发兜底，**所有列表页的卡片都成了滚动容器、表头因此不再吸顶**（含家庭库存这类本来塞得下的表
  —— 阈值量的是容器宽度而不是每张表的宽度，CSS 表达不了"只有真放不下的表才滚动"），
  今日菜单在卡片内横向滚动（更新时间列要滑一下），换掉整页横向滚动条。
- **表头吸顶被顶栏盖住**：`thead th` 的 `top: 0` 撞上 `z-index: 20` 的吸顶顶栏，
  45 行的列表滚到中段时最需要表头却看不见它。改为 `top: var(--topbar-h)` + `z-index: 2`，
  实测滚动 900px 后表头停在 y=72（顶栏底边）而不是钻到它背后。
- **今日菜单的日期列折成两行**：`td.num` 缺 `white-space: nowrap`，日期与更新时间折行把行高
  从 49px 顶到 61px，与其它页不一致。`td.num`/`td.nowrap` 补 nowrap，时间列统一加 `nowrap`；
  用户/家庭/订单页的"昵称 + ID"两行单元格另给 `line-height: 1.35`（行高 56→50）。
- **看板两张趋势图 x 轴末尾两个日期贴死**：`i % step === 0 || i === n - 1` 里补的那个末尾标签
  不受抽样间距约束，实测 09-21 的右边界与 09-22 的左边界落在同一个 x=1344。
  改为末尾锚定的均匀抽样（`axisStep`/`axisLabelAt`），实测相邻标签间距 88/103/132px（1280/1440/1920）。
- **每个列表页的标题出现两次**：顶栏 `#pageTitle` 与卡内 `pageHead` 的 `<h2>` 逐字相同
  （13 处与 NAV label 完全一致）。去掉卡内标题，页头只留说明与右侧操作；
  代价是页头不再有分隔线（紧挨着的 `.toolbar` 自带一条，两条并排只是噪音）。
- **后台登录页的应急入口藏在折叠区里，而它的"可用性探测"会把唯一入口锁在门外**：
  短信网关没接入时「引导登录」是唯一进得去的入口，却要用户先想到"点开收不到验证码？"。
  改成与验证码并列的分段切换后，可用性不再由前端探一次 `POST /api/admin/auth/bootstrap`
  （空令牌：401=已配置，404=没配）决定，改由服务端渲染进 `<body data-bootstrap-login>`
  （`AdminPageController`）。探测版有两个代价，实测都能复现：该端点限流 5 次/分钟、
  且限流计数在 `allow()` 里先自增再比较、不看响应码，于是同一分钟刷新登录页 6 次，
  第 6 次连真正的引导登录也吃 429「操作过于频繁」（`sweep-errors-expired.json`：
  A1-5/A1-6 两条 429）；空令牌又必然 401，控制台里常挂一条看起来像故障的报错。
  服务端本来就知道答案，写进页面即可：一次请求都不用花，也不会误判。
  验证码一侧补一句"本环境可能未接短信网关，没收到验证码就用引导令牌"。
- **登录页在「没接短信网关」的环境里是条死胡同**：`SMS_PROVIDER=noop`（默认）且未开
  `AUTH_DEV_OTP_ENABLED` 时「获取验证码」返回 503「短信网关未配置…」，而未配
  `ADMIN_BOOTSTRAP_TOKEN` 时引导入口按设计整条隐藏 → 页面上没有任何一条能进后台的路，
  也没告诉运维该设哪个变量。现在 503 分支做一次分流：引导登录可用就自动切到「引导令牌」
  分段并说明；不可用就把两个变量写清楚（`AUTH_DEV_OTP_ENABLED=true`，或
  `ADMIN_BOOTSTRAP_TOKEN` + `ADMIN_BOOTSTRAP_PHONE`）。只在服务端明说发不出码时出现。
- **验证码输错被报成「登录已失效，请重新登录」**：登录请求没带 `allow401`，服务端的
  401（验证码不正确）落进了全局登出分支——用户只是打错一位数字，却被清掉会话、被通知
  "登录已失效"，且按钮停在禁用态。登录/发码/引导三处请求改为 `allow401: true`，
  按服务端原话提示。
- **用户页给自己那一行提供「调整角色」，点了必然 403**：服务端硬拒绝改自己的角色
  （`AdminService.setAdminRole` → 403「不能修改自己的角色」，行为有 `AdminRbacTests` 守着），
  前端却照常渲染按钮。后台全量点击巡检的确认路径在管理员自己那一行上连吃两条 403
  （空提交 + 填值提交各一条）。现在 `state.userId` 从 `/api/admin/me` 取，自己的行上按钮
  禁用并带 `title` 说明原因——不删按钮，因为同一列里少一个按钮更让人怀疑是不是看错了行。
  封禁一侧不受影响：能看到这一页的一定是管理员，自己的行本来就不渲染封禁按钮。
- **筛选 chip 顺序不统一**：内容治理（全部在第 2 位）、购物清单与订单（全部在第 1 位）
  与其余 5 个页面的「状态在前、全部在最后」不一致，现统一。

- **做菜完成连点、返回后再点会重复记账并扣库存**：复用 `runGuarded` 覆盖评分面板与保存请求，
  成功后用 `redirectTo` 替换做菜页；跳转失败留在原页时按钮变成「已记录 · 查看记录」，只重试查看。
  保存未确认时保留步骤/计时、不标记上桌、不自动跳走；历史已保存但上桌状态失败时明确提示。
  新增 `cook-finish.test.js`，覆盖连点、保存失败、取消评分、导航失败、菜单回写失败，已接入 CI。
  服务端 `POST /api/cook-history` 另补一层窗口去重（不带评分的纯记录，按 family+recipe+user 去重 60s）：
  连点实测 22ms 内两条都落库、冰箱扣两遍；现在第二条回显已有那条（`pantryDeducted=0`），
  前端照旧拿到一条能渲染的记录。**边界**：窗口是固定 60s；请求级精确仍需客户端幂等键 + 唯一索引。
  写这层去重时踩到一个只有并发才现形的坑：去重判断是**锁定读**（`FOR UPDATE`），紧接着按 id 取回那条
  却是普通快照读——REPEATABLE READ 的快照还停在对家提交之前，同一事务里刚查到的 id 查不出行，
  `EmptyResultDataAccessException` 直接变成给用户的 404（实测：并发两次上桌，一条 200、一条 404）。
  取回那条也加 `FOR SHARE` 后才稳。这条路径是新加的 `simultaneousDoubleClickIsNotA404` 在守：
  它连发三对**并发**连点（一前一后的顺序提交由另一条用例守），反向验证过有牙——把 `FOR SHARE`
  去掉，连跑 3 次全红（`[404, 200]`／`[200, 404]`，日志里 3 次 `cookHistoryById MISS`）；
  加回去 3 次全绿（18 次取回、0 次 MISS）。
  `CommunityWriteRaceTests` 那条两人竞态用例原来拿**同一个账号**发两条并发请求凑"两个家人"，
  现在会被去重吃掉一条，已改成用邀请码拉第二个家人进同一家庭（去重按 user_id 走，两个人才测得到同一行库存）。

- **做菜模式的「下一步 / 完成·记一笔」在折线以下**（做菜主线的主按钮，每一步都要按）：
  根节点写的是 `min-height: 100vh`，容器高度于是由内容决定——步骤大图 257px + 正文 + 配料 + 计时卡
  把页面撑到 911px，而 `.body`/`.stepwrap` 的 `flex: 1` 在自动高度的容器里拿不到收缩目标，
  页脚被顶到屏外。模拟器实测（390×844，iPhone 12/13 档）：`.cook` 高 911、`.footer` 底边 y=911、
  「下一步」底边 y=863，全部落在窗口（844）之外，做菜时得先把页面滚一下才点得到主按钮；
  更矮屏幕尚未单独实测。改成 `height: 100vh` 后实测：`.cook` 844、
  `.footer` 底边 844、「下一步」底边 796（安全区底 810 之上）、页面 `scrollHeight` 844（不再整页滚），
  正文区自己滚（338px）。`id=999999` 的 404 失败态实测仍居中；无步骤空态未实测。
- **菜谱详情首帧把 `undefined` 传给 nav-bar**：`status-bar-height="{{statusBarHeight}}"` 绑的字段
  在 `data` 里没声明（骨架屏那处在 onLoad 之前就渲染），控制台每进一次刷一条
  「expected <Number> but got non-number value. Used 0 instead」，顶栏高度也只能等组件 `attached` 回填、跳一帧。
  补上 `statusBarHeight: 0` 后控制台无 warn（`grep -i warn` 从 2 条命中变 0 条），
  并加 `static-check` 第 31 项钉住（反向验证：删掉这行默认值，门禁当场报 `nav-bar 高度字段没声明`）。
- **做菜模式每步都摆出整份配料，却不告诉你这一步要用哪几样**：7 个 chips 在 5 个步骤里长得一模一样，
  第 4 步「加料酒、生抽、老抽、八角、桂皮」得自己在整排里找。现在按步骤正文匹配高亮这一步点到的料
  （`utils/dish-logic.js` 的 `ingredientsInStep`，纯函数 + 6 组断言）：命中的加描边与底色，
  其余压暗一档；**一步都没命中就整排保持中性**（"大火收汁"这类步骤不许把整排压灰），
  且任何一条配料都不会被隐藏——匹配不到只是不高亮，备菜清单还是完整的。
  实测第 4 步：生抽/老抽/料酒/八角/桂皮 in，五花肉/冰糖 dim；第 5 步（无命中）全中性。
- **计时跑起来后剩余时间只在滚动区里的计时卡上**：做菜时手机常放一臂外，正文区一滚（现在 footer 常驻，
  正文区自己滚）倒计时就看不见了。顶栏那枚「计时」胶囊改成跑着时显示本步剩余时间（`41:58` 实测），
  暂停或未开始时仍是「计时」。
- **菜谱详情的悬浮快捷键压住新的「去晒」按钮**：滚到底后收起悬浮键。
  实测（390×844）：悬浮键 y 520–623、最后一屏的「晒我的作品 / 去晒」那一行 y 537–590，整行被盖住；
  内容到底后一行不会再动，藏起来才是真的不挡（到底了也用不着"一键回顶"）。
  判据用滚动的 `scrollHeight` 与窗口高度比，不写死像素；`_viewHeight` 取不到时退回旧行为（悬浮键常显）。
- **购物清单为空时永远说「今晚的菜都在库存里」，而那句话是编的**：`rebuildShoppingList` 只把菜单里
  各道菜的用料聚合起来、全程不看冰箱，所以"清单为空"根本不可能等于"菜都在库存里"。
  现在按原因分岔：没点菜 → 「今天还没点菜 / 先去菜谱挑几道」；点了菜却没清单 → 「还没有要买的 /
  今天点的菜还没录用料」。统计文案与菜单页菜篮子磁贴副标题共用同一处判据
  （`utils/kitchen.js#shoppingEmptyReason`，6 条断言，含"有清单就不是空态"的反向判据）。
- **厨房总控里没在计时的灶位显示 `--:--`**：读起来像计时器坏了，而这里要回答的是
  「这口灶现在有没有在跑」。改成「未计时」（灰字、小一号），有计时才显示表盘。

- **购物清单的"来自哪道菜"每行重跑一次四表连接**：那条 `GROUP_CONCAT(...)` 相关子查询在
  EXPLAIN 里是 4 个 `DEPENDENT`，清单有几行就跑几遍；改成两条查询 + Java 侧按食材分组，
  顺带解掉 `group_concat_max_len`（实测默认 1024 字节）把来源静默截断的问题。
  `loadPreviousPurchasedMap` 也不再顺带跑一次来源回溯。
- **"今天"有两套定义**：许愿侧用 JVM 的 `LocalDate.now()`、菜单侧用 SQL `CURRENT_DATE`，
  而 JDBC 连接钉了 `serverTimezone=Asia/Shanghai` —— 夜里许的愿会落到菜单看不到的那天。
  统一到数据库时钟（与 `AdminService.metrics` 同一个先例）。
- **前端读写共用一个 10s 超时**：现在 GET 给 15s、写请求保持 10s——写等太久会让人以为没戳上再点一次。
- **并发点赞不再报错**：原来"先查再插"，两个人同一瞬间点同一个帖时第二条撞唯一键变成
  409「记录已存在，请勿重复操作」。现在把唯一键冲突与 InnoDB 死锁一起纳入**事务外重试**
  （`inTxWithDeadlockRetry`，MySQL 对死锁的建议原文就是 "try restarting transaction"）。
  中途两个失败的第一版都记进了清单：`INSERT IGNORE` 会因共享锁互等成死锁；
  `ON DUPLICATE KEY UPDATE` 的受影响行数在 Connector/J 默认 `useAffectedRows=false` 下
  会把"取消点赞"错判成"新点赞"。
- **两个人同时做完同一道菜，两次扣减都在**：冰箱库存原来是读进内存算完写绝对值，
  两边都写回 6、后一次扣减凭空消失。改成带原始值的乐观条件 + 冲突时抛
  `OptimisticLockingFailureException` 让外层开**新事务**（同一事务里重读无效：
  REPEATABLE READ 的快照停在第一次读那一刻）。
- **并发评论不再回显成别人的文字**：回显原来取"该帖最新一条"，改为按生成键取自己那行。
- **点赞计数改为从行重算**，不再 ±1：增量维护会和真实行数漂移，而 feed 按 `like_count` 排序。
- **社区"资源不存在"从 400 改成 404**，与菜谱详情同一语义（重复删除仍故意留 400，
  那是"已删/非本人"合并文案的反探测设计）。
- **社区信息流每次翻页都全表扫一遍收藏数**：`LEFT JOIN (SELECT post_id, COUNT(*) FROM
  community_post_favorite GROUP BY post_id)` 那个派生表没有按本页帖子收敛（EXPLAIN 里是
  `DERIVED ... Using index`），改成按行相关子查询后每页只数 20 次。
- **菜谱列表的临时表**：`cook_history` 的派生表要先物化再哈希连接（`Using temporary`），
  改成两个走 (family_id, recipe_id) 前缀的相关子查询，临时表消失。
- **做菜模式的计时器活不过"关掉再进"**：每步一个计时槽只挂在页面对象上，而厨房总控页早就把它
  存进 storage——同一个"炖着 20 分钟"，一个入口跨路由活着、另一个重进就归零。
  现在复用厨房页的四元组形状落盘（`cook_timers_<recipeId>`），恢复算法收进
  `utils/kitchen.js#restoreTimerSlots` 与厨房页共用一份，并补 7 条断言
  （含"离开期间到点的槽落在 00:00 而不是整槽消失"）。
- **做菜模式只能线性推进**：回头看第 3 步要连点 5 次「上一步」。进度条每段现在可点直跳
  （热区撑到 88rpx，视觉未变）。
- **交互体检看不见 `:active` 这种反馈**：检核器只认 `tap-scale / tap-dim / hover-class`，
  于是"进度段按下提亮"（缩放会让相邻段发抖）被误判成没反馈。已补成第四种合法写法并反向验证。

- **周菜单一个加载态都没有**：首屏先显示统计卡的「0 天 / 0 道 / 0 个」，数据回来才跳成真实数字。
  现在补 `state-loading` 并在加载中隐藏内容块。
- **「我的」页做菜记录的骨架与真卡不同尺寸**（96rpx vs 120rpx），数据回来那一瞬整行会跳；统一到 104rpx。
- **同一个作者在两处不同大小**：社区列表头像 84rpx、帖子详情 92rpx、评论行 72rpx——先给三档命名止住漂移。
- **餐桌页周预览暗卡的圆角 46rpx** 是个没有出处的数，归到刻度上的 `--r-xl`(42rpx)。

- **帖子详情操作栏必然溢出**：可用宽 664rpx、五个胶囊实际需要约 800rpx，胶囊被压扁、
  「收藏 0」竖排、三个胶囊高度 38/22/58px 各不相同。改为不压字、整胶囊换行，
  并把分享/举报降为次级文字动作独占一行（与社区列表页同一层级）。
  同一排的三个字符图标 `❝ ⤴ ` 换成 CSS 画的单色图标。
- **厨房总控的 💡 与开饭横幅的 🎉**：彩色 emoji 吃不到 CSS color，既不跟品牌色也不跟深色档翻。
  💡 换成纯排版的「建议」标签（CSS 画灯泡在 26rpx 下读作气球，截图否掉了），🎉 直接删。

- **周菜单加载失败时整块内容凭空消失**：`state-empty` 没在 `index.json` 声明所以从来没渲染过；
  补上声明后又把错误态从"七天卡片之后"提到 sheet 开头——失败时不再先显示「0 天 / 0 道」的统计卡。
- **冰箱「匹配失败」被显示成「还没匹配到菜谱」**（`matchFailed` 置了位却没人读），
  并给「删除食材」补上与「添加」对称的 `已删除` 回执。
- **菜谱页重复加菜时静默跳页**（点"加入"却换页、什么也没加），现在先说明"已在今日菜单"。
- **帖子详情只在 `onLoad` 拉一次**：去别处点赞、别人补评论后回来看还是旧数据；
  现在 `onShow` 静默补一次（不闪骨架屏、下架时不清空整屏）。

- **忌口过滤在服务端永远不可能命中**：原来拿成员标签去和菜谱口味标签做**相等**比较
  （库里是"酸辣/麻辣"，成员能选的是"辣"），于是菜谱列表已过滤、首页推荐照推麻婆豆腐。
  改为与前端同一套"菜名+菜系+口味标签"关键词规则，并用测试锁住两端词典一致。
- **三处"加载失败"被显示成"还没有数据"**：家庭成员页（网络一抖就说家里没人）、
  反馈页（说你没提过反馈）、导入页（失败重试分支写好了却从没被触发）。
- **社区发帖的「发布」按钮在 tab 页被自定义 tabBar 永久遮住**；社区/成员页改用全站同一套
  骨架与空态插画。
- **做菜模式计时器**：每步一个槽，换步后继续倒数并在顶栏提示，不再一切步就清空。
- **冰箱"还剩几天"差一天**：到期当天曾被判"已过期"，等于让用户扔掉还能吃的食材；改按日历日算。
- **三处 hero 标题换行后两行字互压**（登录页 / 菜谱详情 / 我的页），以及社区、消息页
  导航标题被右侧按钮压掉最后一个字。
- **菜谱搜索副文案谎称"按相关度排序"**（默认综合并不做任何相关性计算）。
- **三处出站 HTTP 没有超时**：微信接口卡住会拖死全站请求；现在连接 3s / 读取 8s。
- **带签名的绝对图片链接会被原样入库**（7 天后全家图片集体 404）；上传签名过期时间改为按 TTL 分桶，
  `/uploads/**` 补 `Cache-Control`，菜图才真正被缓存。
- **清空忌口后不要把普通成员标成"管理员 · 家庭创建者"**（判据用错了 `isSelf`）。
- 品牌色底上的图标误用会随主题翻转的 `--c-surface` 描边，深色档翻成深灰几乎看不见（共 13 条声明）。
- 社区帖卡的动作图标不再用系统 emoji：`♡ ☆ ⚠` 统一追加文字呈现选择符（否则 iOS/Android 常渲染成彩色 emoji，吃不到 CSS color 也就不跟深色档翻），`💬` 改用 CSS 画气泡。并新增 `static-check` 第 17 项，禁止 `content:` 使用非 BMP emoji 码位或漏掉文字呈现的符号区字符。
- 大字模式（无障碍）此前只有 29/37 页接上线，做菜模式与菜谱详情都在漏网之列——已全量补齐。
- `cook_history` 按家庭维度查询缺索引导致全表扫；补两条索引并给出可重复执行的迁移脚本。
- **首页看板把 100 条帖子连 JOIN 全捞出来，只为展示 4 条**：`/api/home/dashboard` 的「社区精选」
  改为直接取第 1 页 4 条（SQL 结尾从 `LIMIT 100` 变成 `LIMIT 4`，按 MySQL 通用查询日志实测确认），
  展示结果逐条不变（`CoreFlowTests#dashboardFeaturedPostsMatchTheFeedHead` 锁住与 feed 头部等价）。

### Changed

- `docker-compose.yml` 与 `.env.example`：`UPLOAD_ACCESS_SECRET` 变为必填（生产留空直接启动失败）。
- 迁移脚本从两条变三条（新增 `migrate-cook-history-family-index.sql`），**都还没在生产库执行**。

### 已知待办（部署方，缺了会被提审驳回）

- 已备案的 HTTPS 域名填进 `miniapp/utils/env.js`，并同步小程序后台的服务器域名白名单。
- `miniapp/utils/legal-config.js` 里的运营者姓名与联系方式仍是 `【】` 占位符。
- 真实短信网关（`SMS_PROVIDER=noop` 时没人能绑手机号，`/admin` 也进不去）。
- 在生产库执行上面三条迁移。
- 真机验收三项：相册授权弹窗、深色模式整体回归、小屏弹层内滚动。
